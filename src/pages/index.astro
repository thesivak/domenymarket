---
import Layout from '../layouts/Layout.astro';
import domains from '../data/domains.json';
---

<Layout title="DomenyMarket.cz | Prémiové české domény na prodej">
  <!-- Navigation -->
  <nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-lg border-b border-gray-200">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="/" class="flex items-center space-x-2">
          <span class="text-2xl">🌐</span>
          <span class="font-bold text-xl text-gray-900">DomenyMarket<span class="text-blue-600">.cz</span></span>
        </a>
        <a href="#contact" class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-lg font-medium transition-colors">
          Kontakt
        </a>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="pt-32 pb-20 px-4 bg-gradient-to-br from-blue-50 via-white to-emerald-50">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-gray-900 mb-6 leading-tight">
        Najděte perfektní
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-emerald-500">.cz doménu</span>
        pro váš business
      </h1>
      <p class="text-xl text-gray-600 mb-10 max-w-2xl mx-auto">
        Nabízíme pečlivě vybrané české domény s historií, zpětnými odkazy a potenciálem pro růst vašeho podnikání.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#domains" class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-xl font-semibold text-lg transition-all hover:shadow-lg hover:shadow-blue-200">
          Prohlédnout domény →
        </a>
        <a href="#how-it-works" class="bg-white hover:bg-gray-50 text-gray-700 px-8 py-4 rounded-xl font-semibold text-lg border border-gray-200 transition-all">
          Jak to funguje
        </a>
      </div>
    </div>
  </section>

  <!-- Stats -->
  <section class="py-12 bg-white border-y border-gray-100">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
        <div>
          <div class="text-3xl font-bold text-blue-600">{domains.length}+</div>
          <div class="text-gray-600 mt-1">Domén v nabídce</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-blue-600">100%</div>
          <div class="text-gray-600 mt-1">České .cz domény</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-blue-600">24h</div>
          <div class="text-gray-600 mt-1">Rychlý převod</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-blue-600">🔒</div>
          <div class="text-gray-600 mt-1">Bezpečná transakce</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Domains List -->
  <section id="domains" class="py-20 px-4">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">Domény na prodej</h2>
        <p class="text-gray-600 text-lg">Vyberte si z naší nabídky kvalitních českých domén</p>
      </div>
      
      <div class="grid md:grid-cols-2 gap-6">
        {domains.map((domain) => (
          <div class={`bg-white rounded-2xl border ${domain.premium ? 'border-blue-200 shadow-lg shadow-blue-50' : 'border-gray-200'} p-6 hover:shadow-xl transition-all duration-300 group`}>
            {domain.premium && (
              <div class="inline-flex items-center bg-gradient-to-r from-amber-400 to-orange-400 text-white text-xs font-bold px-3 py-1 rounded-full mb-4">
                ⭐ PREMIUM
              </div>
            )}
            <div class="flex items-start justify-between mb-4">
              <div>
                <h3 class="text-2xl font-bold text-gray-900 group-hover:text-blue-600 transition-colors">
                  {domain.domain}
                </h3>
                <span class="inline-block mt-2 text-sm text-gray-500 bg-gray-100 px-3 py-1 rounded-full">
                  {domain.category}
                </span>
              </div>
              <a 
                href={`#contact`} 
                class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-lg font-medium transition-colors shrink-0"
              >
                Mám zájem
              </a>
            </div>
            <p class="text-gray-600 mb-4">{domain.description}</p>
            <div class="flex flex-wrap gap-2">
              {domain.highlights.map((highlight) => (
                <span class="text-sm text-emerald-700 bg-emerald-50 px-3 py-1 rounded-full">
                  ✓ {highlight}
                </span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section id="how-it-works" class="py-20 px-4 bg-gray-50">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">Jak to funguje</h2>
        <p class="text-gray-600 text-lg">Jednoduchý proces ve 3 krocích</p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-white rounded-2xl p-8 text-center shadow-sm">
          <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6">
            1️⃣
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Vyberte doménu</h3>
          <p class="text-gray-600">Prohlédněte si naši nabídku a vyberte doménu, která se hodí pro váš projekt.</p>
        </div>
        <div class="bg-white rounded-2xl p-8 text-center shadow-sm">
          <div class="w-16 h-16 bg-emerald-100 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6">
            2️⃣
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Kontaktujte nás</h3>
          <p class="text-gray-600">Napište nám email s vaší nabídkou. Odpovíme do 24 hodin.</p>
        </div>
        <div class="bg-white rounded-2xl p-8 text-center shadow-sm">
          <div class="w-16 h-16 bg-amber-100 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6">
            3️⃣
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Převod domény</h3>
          <p class="text-gray-600">Po dohodě provedeme bezpečný převod domény na váš účet.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="py-20 px-4 bg-gradient-to-br from-blue-600 to-blue-800 text-white">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl sm:text-4xl font-bold mb-6">Máte zájem o některou doménu?</h2>
      <p class="text-blue-100 text-lg mb-10">
        Napište nám a dejte nám vědět, o kterou doménu máte zájem a jakou cenu nabízíte. Každou nabídku pečlivě zvážíme.
      </p>
      <a 
        href="mailto:domeny@domenymarket.cz?subject=Zájem o doménu" 
        class="inline-flex items-center bg-white text-blue-600 hover:bg-blue-50 px-8 py-4 rounded-xl font-bold text-lg transition-all hover:shadow-lg"
      >
        📧 domeny@domenymarket.cz
      </a>
      <p class="mt-6 text-blue-200 text-sm">Odpovídáme do 24 hodin</p>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-8 px-4 bg-gray-900 text-gray-400">
    <div class="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-center gap-4">
      <div class="flex items-center space-x-2">
        <span class="text-xl">🌐</span>
        <span class="font-semibold text-white">DomenyMarket.cz</span>
      </div>
      <p class="text-sm">© 2026 DomenyMarket.cz. Všechna práva vyhrazena.</p>
    </div>
  </footer>
</Layout>
